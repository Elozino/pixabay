import { ViewStyle } from "react-native";

export const globalStyles = {
  container: {
    flex: 1,
  },
  hstack: {
    justifyContent: "space-between",
    alignItems: "center",
    flexDirection: "row",
  } as ViewStyle,
};
